package org.example.e9.operators;

public class ProgramsOnOperators {
    public static void main(String[] args) {
        //convert seconds to minutes and print
        //66 seconds = 1 min and 6 seconds
        long seconds = 66;
        long min = seconds / 60;
        long reminderSeconds = seconds % 60;
        System.out.println(seconds + " seconds = " + min + " min and " + reminderSeconds + " seconds");

        //set bits
    }
}
